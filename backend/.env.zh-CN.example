# 必填
DATABASE_URL=postgres://localhost/neogate
PUBLIC_BASE_URL=http://127.0.0.1:8080
SITE_NAME=NeoGate
ADMIN_TOKEN_SECRET=change-me-admin-token-secret-in-production
UPSTREAM_SECRET_KEY=change-me-upstream-secret-key-in-production

# 仅 Docker Compose 需要
# POSTGRES_PASSWORD=change-me-postgres-password
# POSTGRES_DB=neogate
# POSTGRES_USER=neogate
# BACKEND_PORT=8080

# 常用可选设置
# BIND_ADDR=127.0.0.1:8080
# CORS_ALLOWED_ORIGINS=*
# 默认为 true，因为后端通常运行在已设置 X-Forwarded-For / X-Real-IP 的反向代理（nginx）之后；
# 仅当后端无可信代理直接对外暴露时设为 false。
# TRUST_PROXY_HEADERS=true

# 部署模式
# RUNTIME_MODE=standalone
# PROCESS_ROLE=all
# REDIS_URL=redis://127.0.0.1:6379/

# 请求和数据库限制
# REQUEST_TIMEOUT_SECONDS=120
# RELAY_BODY_LIMIT_BYTES=67108864
# 上游额度耗尽后冷却该密钥 10 分钟，再允许重新选择。
# QUOTA_COOLDOWN_SECONDS=600
# DB_POOL_MAX_CONNECTIONS=10
# 2GB 服务器高吞吐队列基线。
# USAGE_QUEUE_SIZE=32768
# USAGE_FLUSH_INTERVAL_MS=200
